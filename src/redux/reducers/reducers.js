import { GET_TOKEN } from '../actions/actionType';

const initialState = {
    token: ''
};

export function existingLangList(state = initialState, action) {
    
    switch (action.type) {
    
        default:
            return state;
    }
}
